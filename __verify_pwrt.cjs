const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); (async () = const tables = await prisma.$queryRawUnsafe('SELECT table_name FROM information_schema.tables WHERE table_schema = ''public'' AND table_name = ''password_reset_tokens'''); const ext = await prisma.$queryRawUnsafe('SELECT extname FROM pg_extension WHERE extname = ''pgcrypto'''); console.log('TABLE_CHECK=', tables); console.log('EXT_CHECK=', ext); await prisma.$disconnect(); })().catch(async (e) = console.error(e); await prisma.$disconnect(); process.exit(1); });  
